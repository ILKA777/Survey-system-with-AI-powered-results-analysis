// Фича-флаги. Викторина (правильные ответы) пока не поддержана бэкендом —
// прячем её из UI, но код CreateQuiz.jsx и quiz-ветки Join.jsx сохраняем.
// Чтобы вернуть викторину в фазе 2 — поставить quiz: true.
export const FEATURES = {
  quiz: false,
  // AI-генерация опросов: бэк создаёт черновик сразу (без возможности
  // отредактировать перед сохранением), поэтому пока прячем. Код CreatePoll
  // и src/api/ai.js сохранены — вернуть = поставить true.
  aiGenerate: false,
}
